# cache_plus_federation (L2)

## Story

Cache L4 federation between nodes

## Lesson

Two-plane integration (legacy tier2, now first-class curriculum app).

## Run

```bash
uv run mpreg-example run cache_plus_federation
```

## What it proves

- tier2 `cache_plus_federation` invariants hold

## Architecture

```text
mpreg-example → tier2.cache_plus_federation
```

## Non-claims

- Integration drill, pair with product apps for narrative.

## Production exit ramp

- Legacy mpreg demo tier2 routes through mpreg-example suite.
