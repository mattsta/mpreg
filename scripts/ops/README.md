# Supported operator scripts

| Script                 | Purpose                                               |
| ---------------------- | ----------------------------------------------------- |
| `scrape_prometheus.sh` | Curl `/metrics/prometheus` with optional bearer token |
| `doctor.sh`            | Wrapper around `mpreg doctor`                         |

These are thin wrappers over the CLI for shell automation and runbooks.
